import * as React from "react";
import * as SorobanClient from "soroban-client";

const Swap = ({
  server,
  address,
}: {
  server: SorobanClient.Server;
  address: string;
}) => {};
export default Swap;
