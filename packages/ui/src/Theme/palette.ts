import { PaletteOptions } from "@mui/material";

const Palette: PaletteOptions = {
  mode: "dark",
  background: {
    default: "#131517",
  },
  primary: {
    main: "#E2571C"
  }
};

export default Palette;
